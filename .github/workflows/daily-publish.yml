name: 小红书宠物内容自动化

# 触发条件：每天早上6点和晚上8点（北京时间）
on:
  schedule:
    # UTC时间22点 = 北京时间次日6点（早间内容）
    - cron: '0 22 * * *'
    # UTC时间12点 = 北京时间20点（晚间内容）
    - cron: '0 12 * * *'
  # 手动触发测试
  workflow_dispatch:
    inputs:
      post_type:
        description: '发布类型 (morning/evening/both)'
        required: false
        default: 'both'

env:
  PYTHON_VERSION: '3.11'
  TZ: Asia/Shanghai

jobs:
  generate-content:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      # 1. 检出代码
      - name: 检出代码
        uses: actions/checkout@v4

      # 2. 设置Python环境
      - name: 设置Python ${{ env.PYTHON_VERSION }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      # 3. 安装依赖
      - name: 安装Python依赖
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # 4. 配置环境变量
      - name: 配置环境变量
        run: |
          echo "AI_PROVIDER=${{ secrets.AI_PROVIDER }}" >> $GITHUB_ENV
          echo "OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }}" >> $GITHUB_ENV
          echo "OPENAI_MODEL=${{ secrets.OPENAI_MODEL }}" >> $GITHUB_ENV
          echo "VOLCANO_API_KEY=${{ secrets.VOLCANO_API_KEY }}" >> $GITHUB_ENV
          echo "VOLCANO_API_SECRET=${{ secrets.VOLCANO_API_SECRET }}" >> $GITHUB_ENV
          echo "VOLCANO_MODEL=${{ secrets.VOLCANO_MODEL }}" >> $GITHUB_ENV
          echo "IMAGE_API_KEY=${{ secrets.IMAGE_API_KEY }}" >> $GITHUB_ENV

      # 5. 生成早间内容
      - name: 生成早间宠物内容
        if: contains(github.event.inputs.post_type || 'both', 'morning')
        run: |
          echo "🌅 开始生成早间内容..."
          python scripts/content_generator.py --type morning

      # 6. 生成晚间内容
      - name: 生成晚间宠物内容
        if: contains(github.event.inputs.post_type || 'both', 'evening')
        run: |
          echo "🌙 开始生成晚间内容..."
          python scripts/content_generator.py --type evening

      # 7. 保存记录
      - name: 保存发布记录
        run: |
          echo "💾 保存记录..."
          python scripts/save_records.py

      # 8. 提交内容到仓库
      - name: 提交今日内容
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config init.defaultBranch main
          git add content/
          git add data/records/
          git status
          if [ -n "$(git diff --cached --name-only)" ]; then
            git commit -m "📝 $(date +%Y-%m-%d) 自动更新宠物内容"
            git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }} HEAD:master
            echo "内容已提交到仓库"
          else
            echo "没有新内容需要提交"
          fi

      # 9. 发送完成通知
      - name: 发送完成通知
        if: always()
        run: |
          echo "✅ 内容生成完成!"
          echo "💡 提示: 请在本地运行 'python scripts/publisher.py --platform all --local' 发布到小红书和公众号"
